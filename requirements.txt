# Keep in sync with pyproject.toml — prefer editable install:
#   pip install -e ".[dev]"
#
# Or pin deps directly:
confluent-kafka==2.8.0
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.6.1
Flask==3.1.0
flask-cors==5.0.0
gunicorn==23.0.0
python-dotenv==1.0.1
pytest==8.3.4
pytest-cov==6.0.0
prometheus-client==0.21.1
redis==5.2.1
structlog==24.4.0
tenacity==9.0.0
httpx==0.28.1
Werkzeug==3.1.3

# After clone, install the package itself:
# -e .
